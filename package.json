{
  "name": "femida",
  "version": "1.0.2",
  "dependencies": {
    "firebase": "^9.15.0",
    "git": "^0.1.5"
  },
  "devDependencies": {
    "onchange": "^7.1.0"
  },
  "scripts": {
    "Обновление": "git add . && git status && git commit -m 'commit' && git push https://github.com/Fantik13/femida",
    "build": "",
    "deploy": "firebase deploy",
    "publish": "npm publish",
    "init": "git init",
    "add": "git add .",
    "status": "git status",
    "commit": "git commit -m 'commit'",
    "push": "git push",
    "pull": "git pull ."
  }
}
